/* ----------------------------
   Reliablestore - Modern Desktop-first CSS
   Brand: #FE9900
   Replace your root styles.css with this file
   ---------------------------- */

:root{
  --brand: #FE9900;
  --brand-dark: #d97f00;
  --bg: #f6f8fb;
  --card: #ffffff;
  --muted: #6b7280;
  --ink: #0b0b0b;
  --dark: #0f1720;
  --maxw: 1600px; /* max content width for large desktops */
  --radius: 12px;
  --easing: 200ms;
  --shadow-1: 0 6px 20px rgba(2,6,23,0.06);
  --shadow-2: 0 2px 8px rgba(2,6,23,0.04);
  --edge-gap: 28px;
  --container-pad: 24px;
  --system-font: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
}

/* Reset / base */
* { box-sizing: border-box; }
html,body { height:100%; }
body {
  margin: 0;
  font-family: var(--system-font);
  background: var(--bg);
  color: var(--ink);
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
  line-height:1.45;
  font-size:16px;
}

/* Utility containers */
.wrap {
  max-width: var(--maxw);
  margin: 0 auto;
  padding: 0 var(--container-pad);
}

/* Full-bleed inner (for hero, bands) */
.full-bleed,
.hero-full,
.features-band,
.site-footer {
  width: 100%;
  margin: 0;
  padding: 0;
}
.full-bleed-inner,
.hero-inner,
.features-wrap,
.site-footer .wrap {
  max-width: var(--maxw);
  margin: 0 auto;
  padding: 32px var(--container-pad);
}

/* Header (sticky, simple) */
.site-topbar {
  background: var(--dark);
  color: #fff;
  position: sticky;
  top: 0;
  z-index: 9999;
  box-shadow: none;
  transition: box-shadow var(--easing), background-color var(--easing);
}

/* inner header layout */
.site-topbar .wrap {
  display: flex;
  align-items: center;
  gap: 20px;
  padding: 14px var(--container-pad);
}

/* Brand/logo */
.brand { display:inline-block; line-height:0; }
.brand img { height:40px; display:block; }

/* Nav */
.site-nav {
  display:flex;
  gap:20px;
  margin-left: 12px;
  align-items:center;
}
.site-nav a {
  color: #d2dae6;
  text-decoration:none;
  font-size:14px;
  padding:6px 4px;
  transition: color 160ms;
}
.site-nav a:hover { color: var(--brand); }

/* Header actions (login + cart) */
.header-actions {
  margin-left:auto;
  display:flex;
  gap:12px;
  align-items:center;
}
.btn-link {
  color: var(--brand);
  text-decoration:none;
  font-size:14px;
  padding:6px 10px;
  border-radius:8px;
}
.cart {
  background:#06a6ff;
  color:#fff;
  padding:8px 12px;
  border-radius:8px;
  text-decoration:none;
  font-size:14px;
  display:inline-flex;
  gap:8px;
  align-items:center;
}

/* Sticky shadow when scrolled (applied by JS optionally) */
.site-topbar.scrolled { box-shadow: var(--shadow-1); }

/* HERO - full-bleed */
.hero-full {
  background-size: cover;
  background-position: center;
  min-height: 320px;
  display:flex;
  align-items:center;
  position: relative;
}
.hero-overlay { position:absolute; inset:0; background: linear-gradient(180deg, rgba(255,255,255,0.72), rgba(255,255,255,0.85)); pointer-events:none; }
.hero-inner {
  position: relative;
  z-index:2;
  text-align:center;
  padding:64px var(--container-pad);
}
.hero-inner h1 {
  font-size:46px;
  margin:0 0 12px;
  font-weight:700;
}
.hero-inner p {
  color:var(--muted);
  font-size:18px;
  margin:0 auto 18px;
  max-width:900px;
}

/* Search inside hero */
#searchForm { display:flex; gap:8px; justify-content:center; align-items:center; margin-top:18px; }
#searchForm input[type="search"] {
  width:66%;
  min-width:360px;
  padding:10px 12px;
  border-radius:8px;
  border:1px solid #e6e9ef;
  background:#fff;
}
#searchForm button {
  background: var(--brand);
  color: #fff;
  border: 0;
  padding:10px 14px;
  border-radius:8px;
  cursor:pointer;
}
#searchForm button:hover { background: var(--brand-dark); }

/* CHIP LINKS (category buttons) */
.chip {
  display:inline-block;
  background: #fff;
  border-radius:999px;
  padding:8px 14px;
  color:var(--brand);
  text-decoration:none;
  box-shadow: var(--shadow-2);
  font-weight:600;
  font-size:14px;
}

/* FEATURES / PRODUCTS BAND (grid) */
.features-band { background: transparent; }
.features-wrap {
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap:28px;
  padding: 28px var(--container-pad);
}

/* Feature / card */
.feature {
  background: var(--card);
  border-radius: var(--radius);
  padding: 20px;
  box-shadow: var(--shadow-2);
  display:flex;
  flex-direction:column;
  align-items:flex-start;
  min-height: 260px;
  transition: transform 180ms ease, box-shadow 180ms ease;
}
.feature img {
  width:100%;
  max-height:160px;
  object-fit:contain;
  border-radius: 8px;
  margin-bottom:12px;
  background: linear-gradient(180deg, #fff, #fafafa);
}
.feature h3 { margin:0 0 6px; font-size:16px; font-weight:700; color:var(--ink); }
.feature p { margin:0 0 12px; color:var(--muted); font-weight:700; }

/* Add to cart button */
.add-btn {
  margin-top:auto;
  width:100%;
  background: #06a6ff;
  color:#fff;
  border:0;
  padding:10px 12px;
  border-radius:8px;
  cursor:pointer;
}
.add-btn:hover { opacity:0.95; transform: translateY(-1px); }

/* Footer */
.site-footer {
  background: var(--dark);
  color: #d1d5db;
  padding: 48px 0;
}
.site-footer .wrap {
  display:flex;
  gap:40px;
  justify-content:space-between;
  flex-wrap:wrap;
}
.site-footer a { color:#c7ceda; text-decoration:none; display:block; margin-bottom:8px; }
.site-footer p { margin:0; }

/* Utility helpers */
.container-row { display:flex; gap:20px; align-items:flex-start; }
.center { text-align:center; }
.hidden { display:none !important; }
.kicker { color:var(--muted); font-size:13px; }

/* Accessibility focus */
a:focus, button:focus, input:focus { outline: 3px solid rgba(254,153,0,0.15); outline-offset: 2px; }

/* PERFORMANCE: prefers-reduced-motion */
@media (prefers-reduced-motion: reduce) {
  .feature, .add-btn, #searchForm button { transition: none !important; transform: none !important; }
}

/* =====================
   Desktop-first responsiveness
   We'll keep desktop polish; minimal mobile rules included so page is usable
   (User requested to optimize mobile later)
   ===================== */

@media (max-width: 1400px) {
  .features-wrap { grid-template-columns: repeat(3, 1fr); gap:22px; }
  .hero-inner h1 { font-size:40px; }
}

@media (max-width: 1100px) {
  .features-wrap { grid-template-columns: repeat(2, 1fr); }
  #searchForm input[type="search"] { width: 58%; min-width: 320px; }
  .hero-inner { padding:48px 20px; }
}

@media (max-width: 880px) {
  .features-wrap { grid-template-columns: 1fr; max-width: 840px; margin: 0 auto; }
  .hero-inner h1 { font-size:34px; text-align:left; }
  .hero-inner p { text-align:left; }
  #searchForm input[type="search"] { width: 100%; min-width: 0; }
  #searchForm { flex-direction: column; gap:12px; align-items:stretch; }
  .site-topbar .wrap { flex-wrap:wrap; gap:12px; }
  .site-nav { order: 3; width:100%; display:flex; gap:12px; overflow-x:auto; padding-bottom:6px; }
  .site-nav a { white-space:nowrap; padding:6px 8px; display:inline-block; }
}

/* Very small screens */
@media (max-width: 520px) {
  .hero-inner h1 { font-size:24px; }
  .hero-inner p { font-size:14px; }
  .brand img { height:34px; }
  .site-topbar .wrap { padding:10px 12px; }
  .chip { padding:8px 10px; font-size:13px; }
}

/* LESS IMPORTANT: small visual polish */
.feature:hover { transform: translateY(-6px); box-shadow: 0 18px 40px rgba(2,6,23,0.08); }
.chip:hover { transform: translateY(-2px); box-shadow: var(--shadow-2); }

/* Simple .nav-link override to ensure login text uses brand color */
.btn-link { color: var(--brand); }
.btn-link:hover { color: var(--brand-dark); }

/* Make links use brand by default in content areas */
a, a:visited { color: var(--brand); }

/* Small print / copyright */
.copyright { color:#9aa3b5; font-size:13px; }

/* End of file */
